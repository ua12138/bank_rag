# Ingestion Skeleton

对应真实文件：`ingestion/document_parser.py`、`ingestion/chunker.py`

```python
def parse_document(path):
    # by suffix -> parse text
    pass

def split(text):
    # recursive / semantic
    pass
```
