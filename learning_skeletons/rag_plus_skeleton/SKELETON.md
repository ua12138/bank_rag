# RAG_PLUS Skeleton

对应真实文件：`RAG_PLUS/main.py`、`RAG_PLUS/router.py`、`RAG_PLUS/redis_runtime.py`

```python
def plus_query(req):
    # auth -> rate limit -> inflight slot
    # route -> cache -> adaptive ask
    # release slot
    pass
```
