# Storage Skeleton

对应真实文件：`storage/rag_repository.py`、`storage/metadata_store.py`

```python
def ingest_document(file_path):
    # parse -> clean -> chunk
    # save metadata/chunks
    # upsert vector + rebuild bm25
    pass
```
